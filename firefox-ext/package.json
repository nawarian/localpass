{
  "name": "localpass-firefox",
  "version": "1.0.0",
  "description": "LocalPass Firefox Extension – local-first password management",
  "type": "module",
  "source": "manifest.json",
  "scripts": {
    "dev": "parcel watch manifest.json",
    "build": "parcel build manifest.json",
    "package": "npm run build && cd dist && npx web-ext build",
    "lint": "npx web-ext lint"
  },
  "dependencies": {
    "@localpass/core": "^1.0.0"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.14.0",
    "@types/firefox-webext-browser": "^120.0.0",
    "buffer": "^6.0.3",
    "parcel": "^2.14.0",
    "process": "^0.11.10",
    "typescript": "^5.7.0",
    "web-ext": "^8.5.0"
  }
}
